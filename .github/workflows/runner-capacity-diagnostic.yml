name: Hosted Runner Capacity Diagnostic

on:
  push:
    branches:
      - diag/hosted-runner-capacity
    paths:
      - '.github/workflows/runner-capacity-diagnostic.yml'

jobs:
  probe:
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        os: [ubuntu-latest, ubuntu-slim, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 3
    steps:
      - name: Probe runner assignment
        shell: bash
        run: |
          echo "os=${{ matrix.os }}"
          echo "runner=${{ runner.name }}"
          date -u
          sleep 20
