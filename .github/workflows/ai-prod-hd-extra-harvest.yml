name: AI Prod HD Extra Gameplay Harvest (legacy manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_manual_only:
    name: Legacy HD test (manual only)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Explain legacy workflow
        run: |
          echo "This legacy native-stream experiment is intentionally manual-only."
          echo "The active production workflow is ai-prod-gameplay-harvest.yml."
